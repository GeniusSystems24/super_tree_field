name: super_tree_field
description: >
  Super Tree — a GeniusLink design-system Flutter package providing a
  recursive, generic, keyboard-first hierarchy component for arbitrary
  typed data. Supports search, expansion, selection, inline editing,
  drag-and-drop, custom cells, scrolling, LTR/RTL, and light/dark themes.

# package version
version: 1.0.1+2

# package homepage, repository and issue tracker
homepage: https://geniussystems24.github.io/super_tree_field
repository: https://github.com/GeniusSystems24/super_tree_field
issue_tracker: https://github.com/GeniusSystems24/super_tree_field/issues

topics:
  - flutter
  - widgets
  - treeview
  - hierarchy
  - accessibility

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter
  # Shared GeniusLink design-system foundation (theme, tokens, type, widgets).
  super_core: ">=3.3.0 <4.0.0"
  super_form_field: ">=1.8.2 <2.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo-mark.png
    - assets/logo-mark-white.png
  # Drop brand .ttf files under assets/fonts/ to match the design system exactly.
  # Families: Manrope (display) · Inter (body) · JetBrainsMono (mono) · NotoNaskhArabic
  #
  # fonts:
  #   - family: Manrope
  #     fonts:
  #       - asset: assets/fonts/Manrope-Regular.ttf
  #       - asset: assets/fonts/Manrope-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Manrope-Bold.ttf
  #         weight: 700
  #   - family: Inter
  #     fonts:
  #       - asset: assets/fonts/Inter-Regular.ttf
  #       - asset: assets/fonts/Inter-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Inter-SemiBold.ttf
  #         weight: 600
  #       - asset: assets/fonts/Inter-Bold.ttf
  #         weight: 700
  #   - family: JetBrainsMono
  #     fonts:
  #       - asset: assets/fonts/JetBrainsMono-Regular.ttf
  #   - family: NotoNaskhArabic
  #     fonts:
  #       - asset: assets/fonts/NotoNaskhArabic-Regular.ttf
